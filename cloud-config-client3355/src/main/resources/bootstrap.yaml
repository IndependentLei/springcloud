server:
  port: 3355
spring:
  application:
    name: config-client
  cloud:
    # config客户端配置
    config:
      label: master #分支名称
      name: applition # 配置文件名
      profile: dev #读取后缀名称
      uri: http://localhost:3344 #配置中心地址
      # # http://localhost:3344/master/application-dev.yaml
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
